/**
 * about: Control flow
 * definition:
 *
 * docs:
 */

// for loop

// syntax:

let booksGenre = [
    "Arts & Photography",
    "Business & Money",
    "Law",
    "History",
    "Romance",
];

// for (<initexpression>; <condition>; <updateexpression>) {
// }

// for (let i = 0; i < booksGenre.length; i++) {
//     console.log(booksGenre[i]);
// }

// while (booksGenre.length > 0) {
//     console.log(booksGenre.shift());
//     console.log(booksGenre);
// }

// This loop is saying “run the code in this code block only if the length of the edm array is less than 0. An infinite loop in this situation would be if the condition in the while loop is always true.

// do-while-loop
